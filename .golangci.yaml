version: "2"
run:
  # Skip *_test.go entirely: test files deliberately use non-idiomatic patterns
  # (classic for loops, dead stores for GC tests, sort.Slice, etc.).
  tests: false
linters:
  enable:
    - copyloopvar
    - errcheck
    - errorlint
    - gocheckcompilerdirectives
    - gocritic
    - godot
    - gosec
    - govet
    - ineffassign
    - makezero
    - misspell
    - modernize
    - nolintlint
    - perfsprint
    - prealloc
    - predeclared
    - reassign
    - revive
    - staticcheck
    - unconvert
    - unused
    - wastedassign
  settings:
    gosec:
      # G115 (integer overflow conversion) fires on every width conversion in
      # the VM's value packing; pervasive false positives, suppressed wholesale.
      excludes:
        - G115
    nolintlint:
      require-specific: true # //nolint must name the linter(s) it suppresses
      allow-unused: false # flag //nolint directives that suppress nothing
  exclusions:
    presets:
      - common-false-positives
formatters:
  enable:
    - gofumpt
