all: lint fast

# Static linting of Go source files. See .golangci.yaml for options.
lint:
	golangci-lint run
	go run ./cmd/mvmlint .

# Generate standard library bindings from Go toolchain source.
# Also rebuilds the committed stdlib/src.zip from the sibling
# github.com/mvm-sh/std clone (via the //go:generate directive in
# stdlib/srcfs.go). Run after updating the Go toolchain or the std module;
# the embedded zip is committed, so a fresh clone builds without this step.
generate:
	go generate ./...
	@cd stdlib && for plat in $$(go tool dist list | tr '/' '_'); do \
		os=$${plat%%_*}; arch=$${plat#*_}; \
		go run ../cmd/extract -goos $$os -goarch $$arch \
			-stdlib \
			syscall $$(go env GOROOT)/src/syscall; \
	done

clean_generate:
	@grep -rl "Code generated by cmd/extract" stdlib/core stdlib/ext 2>/dev/null | xargs rm -f
	rm -f stdlib/src.zip

# Run the test suite with the race detector (fast inner loop).
# Kept separate from coverage on purpose: -race combined with -coverpkg=./...
# is superlinear (~65s vs ~13s here) because every package's atomic coverage
# counter gets race-instrumented in the interpreter's hot loops. Race and
# coverage answer different questions at different cadences, so they live in
# separate targets -- do not recombine them.
test:
	go test -race ./interp

# Like `test` but `-short` skips the heavy x/text network integration test
# (~4s under race). The default `all` target uses this for a quick inner loop;
# run `make test` to also exercise the remote-import path.
fast:
	go test -short -race ./interp

# Measure cross-package coverage (no race; see the note on `test`).
cover:
	go test -covermode=atomic -coverpkg=./... -coverprofile=cover.out ./interp

# Open coverage info in browser
cover-html: cover
	go tool cover -html=cover.out

clean:
	rm -f mvm extract demo cover.out
	$(MAKE) -C examples/c clean

.PHONY: all clean clean_generate cover cover-html fast generate lint test
